node_modules
dist
dist-ssr
.DS_Store
*.local

.vercel

# Fichiers de metadonnees macOS (AppleDouble) — jamais de vraies images
._*
.DS_Store

# Sources de design (visuels haute resolution, non utilisees par le site) —
# gardees en local pour retravailler, jamais deployees.
/design

# Vitrine : photos recopiees depuis Ressources 360 au build.
# Le catalogue (src/data/vitrine.json) est versionne vide, et regenere a chaque
# build — inutile de committer son contenu.
/public/vitrine
