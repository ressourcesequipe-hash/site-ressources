node_modules
dist
dist-ssr
.DS_Store
*.local

.vercel

# Fichiers de metadonnees macOS (AppleDouble) — jamais de vraies images
._*
.DS_Store

# Sources de design (visuels haute resolution, non utilisees par le site) —
# gardees en local pour retravailler, jamais deployees.
/design
